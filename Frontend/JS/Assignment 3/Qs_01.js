// Check whether a given number is integer or not.

function isInteger(val) {
    return Number.isInteger(val);
}

console.log(isInteger(5));
console.log(isInteger(5.5));
console.log(isInteger("5"));


// Check if adding 0.1 + 0.2 equals 0.3 and explain why.

console.log(0.1 + 0.2 === 0.3); // false
console.log(0.1 + 0.2);
// Because JavaScript uses floating-point numbers (IEEE 754) to store decimals.
// Find the maximum value of a safe integer in JavaScript.

function maxSafeInt() {
    return Number.MAX_SAFE_INTEGER;
}

console.log(maxSafeInt());
console.log(maxSafeInt());
console.log(maxSafeInt());

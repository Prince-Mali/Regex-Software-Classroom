// Generate a random number between 0 and 1.

function randomNum() {
    return Number(Math.random().toPrecision(2));
}

console.log(randomNum());
console.log(randomNum());
console.log(randomNum());
console.log(randomNum());
